import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    homeHeader: {
        color: "white",
        fontSize: 30,
        fontWeight: "bold",
        letterSpacing: 1,
        alignSelf: 'center',
    },
});

export default styles;